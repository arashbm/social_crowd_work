name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: arashbm/social_crowd_work

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      MIX_ENV: test
      DATABASE_URL: ecto://postgres:postgres@localhost:5432/test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Elixir and Erlang
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.18.4"
          otp-version: "28"

      - name: Cache deps and build
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Run tests
        run: mix test

  build-and-deploy:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Log in to Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      #
      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v1.2.5
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       set -eu
      #
      #       APP_IMAGE="ghcr.io/arashbm/social_crowd_work:latest"
      #
      #       echo "${{ secrets.GHCR_READ_TOKEN }}" | docker login ghcr.io -u "${{ secrets.GHCR_USERNAME }}" --password-stdin
      #
      #       docker pull "$APP_IMAGE"
      #
      #       docker network create social-crowd-work-net 2>/dev/null || true
      #
      #       docker run --rm \
      #         --network social-crowd-work-net \
      #         -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
      #         -e SECRET_KEY_BASE="${{ secrets.SECRET_KEY_BASE }}" \
      #         -e PHX_HOST="${{ secrets.PHX_HOST }}" \
      #         -e PORT="4000" \
      #         "$APP_IMAGE" /app/bin/migrate
      #
      #       docker stop social_crowd_work 2>/dev/null || true
      #       docker rm social_crowd_work 2>/dev/null || true
      #
      #       docker run -d \
      #         --name social_crowd_work \
      #         --network social-crowd-work-net \
      #         --restart unless-stopped \
      #         -p 127.0.0.1:4000:4000 \
      #         -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
      #         -e SECRET_KEY_BASE="${{ secrets.SECRET_KEY_BASE }}" \
      #         -e PHX_HOST="${{ secrets.PHX_HOST }}" \
      #         -e PORT="4000" \
      #         "$APP_IMAGE"
      #
      #       docker image prune -f
      #
      #       docker ps
